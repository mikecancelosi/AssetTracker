<UserControl x:Class="AssetTracker.View.SearchboxField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50"
             d:DesignWidth="400">
    <Grid x:Name="FieldGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="{Binding ActualHeight, ElementName=FieldGrid}" />
        </Grid.ColumnDefinitions>
        <Border BorderThickness="2"
                BorderBrush="AntiqueWhite" />

        <Label x:Name="Value_Label"
               Content=""
               Grid.Column="0"
               HorizontalAlignment="Left" />
        <Button Height="{Binding ActualHeight, ElementName=FieldGrid}"
                HorizontalAlignment="Right"
                Click="Button_Click"
                Grid.Column="2">
            <Image Source="/View/Search.png" />
        </Button>
    </Grid>
</UserControl>
